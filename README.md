# oran-prb-prediction-saint-tabnet
Intelligent Physical Resource Block (PRB) demand prediction for O-RAN network slicing using transformer-based tabular models (SAINT) and TabNet, evaluated with MSE, R², and MAPE.
